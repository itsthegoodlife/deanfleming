[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmd = 'npm ci --production=false'

[phases.build]
cmd = 'npm run build'

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'
HOSTNAME = '0.0.0.0' 